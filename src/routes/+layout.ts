// Server side rendering is the base of this site. The first globe frame arrives
// in the HTML, so the page is never blank before the JavaScript loads.
export const ssr = true;
export const csr = true;
export const prerender = false;
